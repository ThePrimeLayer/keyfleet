# Must be refused: recovery_codes holding actual codes instead of the
# {stored, where} pointer mapping.
version: 1
keys:
  - { id: yk-a, label: "Key", vendor: yubico }
accounts:
  - id: mail
    service: other
    label: "Mail"
    tier: T1
    registrations:
      - { key: yk-a, type: fido2-discoverable }
    recovery_codes:
      - "1234-5678"
      - "8765-4321"
